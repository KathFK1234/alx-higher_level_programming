#!/usr/bin/python3
def islower(c):
    for i in range(97, 123):
        a = chr(i)
        b = str(a)

        if (ord(c) != int()):
            return ("Traceback (most recent call last):")
        if (c is b):
            return (True)
        else:
            return (False)
